export type SessionDataType = {
  school: {
    id: string;
    name: string;
  };
  user: {
    id: string;
    name: string;
    position: string;
    level: string;
  };
  token: string;
};
