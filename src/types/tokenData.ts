export type TokenDataType = {
  user: {
    id: string;
    level: string;
  };
  employee: {
    id: string;
    position: string;
  };
  school: {
    id: string;
    name: string;
  };
};
